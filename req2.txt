Please refactor and fix the following UI, styling, and authentication issues across the codebase:

### 1. Input UI & Password Visibility Fixes
- **Remove Eye Icon from Email Input:** Ensure password visibility toggles (eye icons) strictly render inside password input fields and are completely removed from email input fields across all authentication forms (`login`, `signup`).
- **Form Input Padding:** Add adequate vertical and horizontal internal padding (e.g., `py-2.5 px-3.5` or `p-3`) to all `<input>` elements in login, registration, and user portal forms to enhance text readability and eliminate cramped UI text.

### 2. Authentication & Email Verification
- **Email Verification Integration:** Implement email verification for newly registered users. Configure Django's standard SMTP email backend utilizing Gmail credentials (`EMAIL_HOST = 'smtp.gmail.com'`, `EMAIL_PORT = 587`, `EMAIL_USE_TLS = True`, utilizing App Passwords in `.env`). Send signed activation links or token-based verification emails upon sign-up.
- **Duplicate Email Prevention & Feedback:** Add server-side validation to check if an email already exists in the database during registration. If an existing user attempts to register with an already registered email, catch this error in the view/form validation and display a clear user-facing error notification: `"An account with this email already exists."`

### 3. Visual Consistency Across Forms & Buttons
- **Standardize Doctor & Patient Buttons:** Ensure primary submit buttons (`Sign In`, `Sign Up`, `Register`) across both Doctor and Patient flows share consistent styling. Match Doctor buttons to the Patient primary button style (`background: #0d9488` / `#00a884` teal theme with matching white font color, hover states, border-radius, and typography).

### 4. Smooth Scrolling Navigation
- **Landing Page Navigation:** Add `scroll-behavior: smooth;` to the root HTML or main landing page container (`html { scroll-behavior: smooth; }` or via Tailwind class `scroll-smooth`) so clicking navigation bar anchor links smoothly animates to corresponding landing page sections.

### 5. Floating Badge Cleanup
- **Remove Floating Toast / Mock Card:** On the main landing page header section, locate and remove the floating mock preview card floating near the preview graphic that displays `"Appointment Confirmed - Dr. Johnson · Tomorrow at 2:00 PM"`.